# Track only the two project folders. Everything else in this dir is local scratch.
/*
!/.gitignore
!/taelokim-site/
!/taelo-content-system/

# Common excludes inside tracked folders
.DS_Store
*.log
node_modules/
.env
.env.local

# Raw video takes — too heavy for git, kept local-only
taelo-content-system/raw-takes/*.mov
taelo-content-system/raw-takes/*.mp4
taelo-content-system/raw-takes/*.mkv
taelo-content-system/raw-takes/*.webm

# Claude Code local state — machine-specific paths, recreate per machine
**/.claude/settings.local.json
# Local launch configs / scratch servers — per-machine, not synced
**/.claude/launch.json
**/.claude/serve.py
